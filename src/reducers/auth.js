const initialState = {
    isAuthenticated: false,
    isAuthenticating: false,
}

export default function auth(state = initialState, action = {}) {
    switch (action.type) {

    }
    return state
}
